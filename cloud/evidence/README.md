# Cloud evidence

No ENA or EFA execution evidence exists. AWS identity validation failed with
`InvalidClientTokenId` on 2026-08-08 before any resource or quota query.

When the gates run, store sanitized preflight, infrastructure, testpmd,
forwarder, libfabric, performance, packet-capture, cost, and termination logs
here. Never commit credentials, account IDs, private keys, or reusable tokens.
