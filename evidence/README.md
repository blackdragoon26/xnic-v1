# Evidence directory

Raw output belongs here in timestamped subdirectories created by
`scripts/guest/collect-evidence.sh`. Do not hand-edit raw logs.

`release-gate.md` starts as an unverified checklist. Mark a row passed only when
the linked raw artifact exists.
